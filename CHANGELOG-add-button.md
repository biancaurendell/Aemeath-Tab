# 添加图标按钮隐藏功能移除记录

## 修改内容

已完全删除"添加图标"按钮的单独隐藏功能，使其始终保持可见。现在按钮的显示/隐藏**仅由设置中的"显示图标"选项控制**。

## 删除的功能

1. **右键点击隐藏**
   - ❌ 不再支持右键点击"添加图标"按钮来隐藏它
   - ❌ localStorage 中的 `pixelNewTab.addButtonHidden` 不再被读取

2. **右键恢复功能**
   - ❌ 不再支持右键点击快捷方式行背景来恢复"添加图标"按钮

3. **临时显示逻辑**
   - ❌ 删除了右键点击快捷方式时临时显示添加按钮的功能
   - ❌ 删除了相关的临时状态变量：
     - `addWasHiddenBeforeContext`
     - `addOriginalNextSibling`
     - `addTemporaryShownForId`

4. **临时显示函数**
   - ❌ 删除了 `toggleAddVisibility()` 函数
   - ❌ 删除了 `moveAddButtonAfter()` 函数
   - ❌ 删除了 `restoreAddButtonPosition()` 函数

## 修改后的行为

### 显示规则（现在仅此而已）

1. **常规情况**：按钮基于分页设置显示
   - 如果禁用分页：按钮始终显示
   - 如果启用分页：按钮只在最后一页显示

2. **整体控制**：由"常规设置" → "显示图标"复选框控制
   - ✅ 勾选：显示整个快捷方式行（包括添加按钮）
   - ✅ 取消勾选：隐藏整个快捷方式行

### 用户流程

**之前**：用户可以单独隐藏"添加图标"按钮
**现在**：用户只能通过设置面板中的"显示图标"选项来控制整体显示

## 代码修改位置

**文件**: `src/modules/shortcuts.js`

### renderShortcuts() 函数 (第110-193行)
- 移除了读取 `pixelNewTab.addButtonHidden` 的逻辑
- 移除了特殊处理空快捷方式列表的逻辑
- 现在只根据分页设置决定是否显示按钮

### initShortcuts() 函数 (第419-604行)
- 移除了初始化时读取隐藏状态的代码（第522-525行）
- 移除了右键点击"添加图标"按钮的 contextmenu 事件监听器
- 移除了右键点击快捷方式行的 contextmenu 事件监听器

### openShortcutMenu() 函数 (第235-246行)
- 移除了右键菜单时临时显示按钮的逻辑

### closeShortcutMenu() 函数 (第248-253行)
- 移除了关闭菜单时恢复按钮位置的逻辑

## 影响分析

### 无破坏性改动
- ✅ 快捷方式的添加/编辑/删除功能不受影响
- ✅ 分页功能不受影响
- ✅ 快捷方式排序/拖拽功能不受影响
- ✅ "显示图标"设置仍然有效，控制整个快捷方式行

### localStorage 清理（可选）
旧用户的浏览器中可能仍保存有 `pixelNewTab.addButtonHidden` 键。虽然不会有负面影响，但可以在用户下次访问时忽略它。

## 测试建议

1. ✅ 验证"添加图标"按钮始终显示（除非被分页隐藏）
2. ✅ 验证右键点击按钮不再显示隐藏选项
3. ✅ 验证"显示图标"设置仍然能控制整个快捷方式行
4. ✅ 验证添加新快捷方式仍正常工作
5. ✅ 验证快捷方式菜单（右键快捷方式）仍正常工作

## 版本信息

- **修改日期**: 2026-04-29
- **文件修改**: src/modules/shortcuts.js
- **代码检查**: ✅ 已通过 npm run check:js

