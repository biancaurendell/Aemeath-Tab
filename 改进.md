# 与 lemon-new-tab 的架构和设计对比改进建议

本文基于当前项目结构，以及 `Redlnn/lemon-new-tab` 的 README、目录结构和关键源码做对比。对方项目定位是正式浏览器扩展，技术栈为 WXT + Vue 3 + TypeScript + Pinia + Element Plus；当前项目定位更接近轻量纯静态新标签页，核心由 `index.html`、`styles.css`、`app.js` 三个文件承载。

参考项目：

- GitHub：<https://github.com/Redlnn/lemon-new-tab>
- README 特性：纯本地、高性能、搜索建议、自定义搜索引擎、Bing 每日图/在线图/本地图片视频壁纸、一言、多语言、书签、最常访问、深色模式、设置同步。
- 架构观察：`entrypoints/newtab` 放页面入口与组件，`shared/settings` 放设置 schema、默认值、迁移和 store，`shared/shortcut` 放快捷方式数据，`shared/sync` 放同步逻辑，`entrypoints/newtab/shared/search` 放搜索引擎和建议，`entrypoints/newtab/shared/wallpaper` 放壁纸策略，`locales` 放多语言文案。

## 1. 项目定位对比

| 维度 | 当前项目 | lemon-new-tab | 可借鉴方向 |
| --- | --- | --- | --- |
| 技术形态 | 纯 HTML/CSS/JS，直接打开即可用 | WXT 浏览器扩展，Vue 组件化 | 先保持轻量，但按模块拆分 JS/CSS；未来若要发布扩展，再考虑 WXT |
| 功能重点 | 像素氛围、音乐、歌词、小角色、壁纸、快捷入口 | 搜索效率、快捷访问、浏览器书签、最常访问、主题/壁纸、同步 | 不丢当前审美特色，补足新标签页效率能力 |
| 架构组织 | 单个 `app.js` 承担所有状态、DOM、业务、动画 | store/composable/component 分层 | 先做“文件级模块化”，避免直接上框架 |
| 数据持久化 | `localStorage` + IndexedDB 背景原图 | WXT storage + Pinia + schema 版本迁移 + IndexedDB | 增加配置 schema、版本号和迁移函数 |
| UI 设置 | 一个右侧设置抽屉，表单集中 | 设置页分组：搜索、背景、快捷方式、主题、性能等 | 设置抽屉拆成分组/标签，降低信息密度 |
| 浏览器能力 | 普通网页能力 | `topSites`、`bookmarks`、`storage.sync`、权限弹窗 | 如果做扩展版，可加入书签/最常访问；网页版保留手动快捷方式 |

## 2. 架构层面的改进

### 2.1 拆分 `app.js`，但先不要重构成 Vue

当前 `app.js` 已经超过 1900 行，里面同时包含搜索、设置、壁纸、音乐、歌词、快捷图标、小角色、流星、导入导出、IndexedDB。继续加功能会越来越难定位问题。

建议先拆成原生 ES Module：

```text
app.js                       只做初始化和模块装配
src/state/storage.js          localStorage/IndexedDB 读写、配置版本
src/state/settings.js         默认设置、读取、保存、迁移
src/modules/search.js         搜索引擎、下拉、跳转、后续搜索建议
src/modules/shortcuts.js      快捷图标 CRUD、右键菜单、快捷键
src/modules/background.js     内置壁纸、上传、URL、IndexedDB
src/modules/music.js          播放器、歌单、进度、歌词
src/modules/pet.js            小角色状态机和拖拽
src/modules/effects.js        点击特效、流星、底部音阶
src/ui/settings-panel.js      设置抽屉 UI 事件绑定
src/ui/dialogs.js             添加/编辑快捷方式弹窗
```

这样可以继承 lemon-new-tab 的“按领域分层”思路，但不引入构建系统，仍然保持当前项目易部署、易备份的优点。

### 2.2 建立配置 schema 与迁移机制

lemon-new-tab 的设置有默认值、版本号和迁移逻辑。当前项目现在是多个 `localStorage` key 分散存储，优点是简单，缺点是未来字段改名、默认值变化、导入旧配置时容易出错。

建议新增统一配置对象：

```js
const CURRENT_CONFIG_VERSION = 1;

const defaultConfig = {
  version: CURRENT_CONFIG_VERSION,
  search: {},
  appearance: {},
  shortcuts: [],
  background: {},
  music: {},
  effects: {}
};
```

短期可以继续兼容旧 key：首次启动读取旧 key，合并到新 `pixelNewTab.config`，并保留导入导出。后续新增功能优先写入统一配置，减少全局散落的存取逻辑。

### 2.3 给资源存储加索引和清理策略

当前 IndexedDB 主要保存背景原图。可以借鉴对方壁纸存储思路，把“本地壁纸、暗色壁纸、封面缓存、上传图标”纳入统一资产管理：

- `assets/background.current`
- `assets/background.dark`
- `assets.shortcutIcon.<id>`
- `assets.music.cover.<trackId>`

这样导出配置时可以选择“轻量导出”或“完整导出”，也能避免所有图片都塞进 `localStorage`。

### 2.4 增加性能开关分层

lemon-new-tab 单独有 `perf` 配置，控制透明、模糊、动画等效果。当前项目已经注意到音乐播放时减少逐帧 DOM 和流星动画，这是很好的方向。

建议把性能开关显式化：

- 低功耗模式：关闭流星 canvas、小角色移动、歌词浮层、点击粒子，只保留静态背景和基础搜索。
- 音乐播放优化：播放时自动降级流星和点击粒子频率。
- 透明/模糊开关：设置抽屉、音乐窗口、快捷图标分别可控。
- `prefers-reduced-motion`：系统减少动态效果时自动关闭非必要动画。

## 3. 视觉和交互设计可借鉴点

### 3.1 设置面板分组

当前设置抽屉内容已经比较多，背景、音乐、外观、导入导出混在一个滚动流里。可以参考对方设置页分区，改成抽屉内标签：

- 外观：时间、搜索框、快捷图标位置和透明度、遮罩强度。
- 背景：内置壁纸、上传、URL、未来的视频壁纸。
- 快捷入口：显示开关、添加方式、图标大小、布局行列、快捷键。
- 音乐：音频 URL、本地音频、Meting API、歌词、音阶。
- 性能：动画、流星、小角色、歌词浮层、低功耗模式。
- 数据：导入、导出、重置。

优先做这个，因为它能直接改善可用性，而且不需要改动太多核心逻辑。

### 3.2 搜索体验升级

lemon-new-tab 的搜索重点更偏“效率工具”：搜索建议、自定义搜索引擎、搜索历史。当前项目已有搜索引擎切换，可以继续加：

- 搜索建议：支持 Bing/Google/Baidu，使用防抖和缓存，避免每次输入都请求。
- 自定义搜索引擎：允许用户填写名称、搜索 URL 模板，例如 `https://example.com/search?q=%s`。
- 搜索历史：本地保存最近搜索词，可关闭。
- 键盘操作：上下选择建议，Enter 搜索，Esc 关闭建议。

这会让当前项目从“漂亮主页”更进一步变成“真正高频可用的新标签页”。

### 3.3 快捷入口布局

对方项目支持最常访问、固定快捷方式、分页、Dock、Launchpad。当前项目的快捷入口更轻，但可以借鉴其中一部分：

- 快捷图标行列数可调，而不是固定横排。
- 快捷图标分页，避免用户添加多了以后挤在一起。
- Dock 模式：底部或搜索框下方一排常用图标，和当前像素云层/音乐窗口错开。
- 图标拖拽排序。
- 可选“从浏览器最常访问导入”：仅扩展版可做，普通网页没有权限。

建议优先实现“布局行列数 + 拖拽排序”，比浏览器书签权限更贴合当前静态项目。

### 3.4 壁纸系统

对方壁纸能力包括 Bing 每日图、在线图片 API、本地图片/视频、暗色模式壁纸、动态主题色。当前项目已有内置壁纸、上传和 URL，可以升级：

- 视频壁纸：上传或 URL 支持 `mp4/webm`，设置中明确标注可能增加耗电。
- 每日壁纸源：可选 Bing 每日图或自定义图片 API。
- 壁纸收藏：用户上传/URL 使用过的壁纸进入本地收藏，而不是只保留当前一张。
- 移动端独立壁纸：当前已经有移动默认图，可开放给用户单独设置。
- 背景适配：提供 cover/contain/居中/模糊填充等模式。

动态主题色可以参考 lemon-new-tab 的莫奈取色，但当前项目有明确的暗粉像素风，不建议自动完全改主题色。更适合做“从壁纸取一个辅助色，用于按钮高光/搜索框边缘/歌词光效”的轻量版本。

### 3.5 一言/诗词模块

lemon-new-tab 有“一言/今日诗词”。当前项目已经有强氛围感时间与歌词浮层，可以增加一个低存在感的文字模块：

- 默认关闭，避免破坏当前画面。
- 开启后显示在时间下方或搜索框下方。
- 支持一言、今日诗词、用户自定义句子。
- 音乐播放时可自动隐藏，避免和动态歌词抢视觉焦点。

这个功能很适合当前“柔粉发光、像素梦境”的气质，但要克制。

## 4. 扩展版可选能力

如果未来想把当前项目发布成浏览器扩展，可以重点借鉴 lemon-new-tab 的 WXT 方案：

- 使用 WXT 生成 Chrome/Edge/Firefox 扩展入口。
- `newtab` 作为主页面，`popup` 做小型设置或快捷开关。
- 使用 `chrome.topSites` 读取最常访问。
- 使用 `chrome.bookmarks` 做书签搜索侧栏。
- 使用 `chrome.storage.sync` 做配置同步。
- 用权限弹窗解释为什么需要书签/最常访问权限。

但这应当是第二阶段。当前项目的特色是“一个静态页面就能跑”，直接迁移到扩展工程会增加构建、权限、发布和调试成本。

## 5. 不建议直接照搬的部分

- 不建议马上引入 Vue + Element Plus：当前 UI 已经高度定制像素风，组件库默认样式会带来大量覆盖成本。
- 不建议把所有样式改成通用现代毛玻璃：当前项目辨识度来自像素、暗粉、音乐和小角色。
- 不建议默认开启联网功能：搜索建议、Bing 壁纸、一言都应做成可关闭，并说明来源。
- 不建议把音乐播放器弱化：这是当前项目相对 lemon-new-tab 的独特卖点，应继续强化而不是删掉。
- 不建议一次性做完整同步：先把配置 schema 和导入导出做好，再考虑浏览器同步。

## 6. 推荐落地顺序

### 第一阶段：整理架构，不改变视觉

1. 新增统一配置 schema，兼容迁移旧 `localStorage` key。
2. 拆分 `app.js` 为搜索、背景、快捷入口、音乐、小角色、效果、设置几个模块。
3. 把设置抽屉改成分组或标签页。
4. 增加乱码扫描和简单静态检查脚本，保证中文和 UTF-8 安全。

### 第二阶段：提升新标签页效率

1. 增加自定义搜索引擎。
2. 增加搜索建议与搜索历史。
3. 快捷图标支持拖拽排序、布局行列、分页。
4. 支持快捷图标数据完整导入导出，包括上传图标资产。

### 第三阶段：增强外观系统

1. 支持视频壁纸和壁纸收藏。
2. 支持移动端独立背景设置。
3. 加入轻量取色，用壁纸辅助色微调高光，不覆盖主像素风。
4. 增加低功耗/减少动画模式。

### 第四阶段：可选扩展化

1. 评估是否需要发布为 Chrome/Edge/Firefox 扩展。
2. 若需要，再引入 WXT，保留当前静态版作为 `standalone`。
3. 扩展版再加入浏览器最常访问、书签搜索、`storage.sync`。

## 7. 总结

当前项目已经有比 lemon-new-tab 更强的情绪表达：像素背景、发光时间、小角色、音乐、歌词和点击特效是核心差异。真正值得借鉴的不是它的外观本身，而是它的“新标签页工程化能力”：模块划分、配置版本、设置分组、搜索建议、快捷入口布局、壁纸策略和性能开关。

最稳妥的路线是：保留当前视觉和音乐特色，先用原生模块化把架构拆清楚，再逐步补效率功能。等功能边界稳定后，再决定是否需要扩展化。
