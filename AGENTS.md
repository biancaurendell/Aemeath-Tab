# Pixel New Tab Project Notes

## 当前系统架构/模块划分

- `index.html`：单页新标签页入口，负责页面结构。包含像素时钟、搜索框、快捷图标区、设置弹窗、添加/编辑图标弹窗、小角色、音乐播放器、底部音频动效等主要 DOM。
- `styles.css`：全局视觉系统和响应式样式。包含像素风背景、暗粉蒙尘遮罩、搜索引擎下拉、快捷图标、设置面板、音乐窗口、歌单列表、滚动条、小角色、流星和底部云层等 UI 样式。
- `app.js`：全部交互逻辑。包含时间渲染、搜索引擎选择、搜索跳转、设置持久化、背景上传与 IndexedDB 存储、快捷图标增删改查、右键菜单、小角色随机移动/拖拽、鼠标指针、音乐播放、Meting 歌单拉取与切歌、配置导入导出等功能。
- `assets/`：本地静态资源目录。当前包含小角色 gif、鼠标 cursor 文件和转换后的 PNG/cur 指针资源。
- 本地数据存储：
  - `localStorage`：保存搜索引擎、显示开关、快捷图标、外观参数、音乐链接、Meting API 地址等轻量配置。
  - `IndexedDB`：保存用户上传的原图背景，避免大图塞入 `localStorage` 导致失败。

## 已完成的功能清单

- 像素风新标签页基础界面：背景、暗粉蒙尘遮罩、像素时钟、日期、流星/星星动画、像素云层。
- 搜索功能：支持 Google、Bing、Baidu、DuckDuckGo 切换；支持设置搜索结果在当前页或新标签页打开。
- 搜索引擎选择 UI：已改成参考图风格的圆角像素下拉菜单。
- 时间显示：像素数字风格，可在设置里显示/隐藏，并支持调节透明度、大小和位置。
- 快捷图标：
  - 可添加网址快捷方式。
  - 支持名称、网址、图标颜色、文字图标、上传图标、快捷键。
  - 支持右键菜单打开、修改、删除。
  - “显示图标”开关会隐藏/显示整个快捷图标区域。
- 背景自定义：
  - 支持上传本地图片作为背景。
  - 支持图片 URL。
  - 上传原图保存到 IndexedDB。
  - 支持配置导出/导入迁移背景和设置。
- 小角色：
  - `move.gif` 在主页随机飞行移动。
  - 移动一段时间后随机切换为 `seal.gif` 或 `sigh.gif`。
  - 点击变成 `stare.gif`。
  - 拖拽变成 `excited.gif`。
  - 飞行方向会根据移动方向左右镜像。
- 鼠标指针：
  - 页面内使用自定义普通指针。
  - 拖拽时切换为 Alternate 指针。
  - 已用 32px PNG/cur 降低过大问题。
- 音乐播放器：
  - 右下角音乐播放小窗口。
  - 可折叠/展开，折叠后保留音符按钮。
  - 支持直接音频 URL。
  - 支持本地音频文件临时播放。
  - 支持小窗口和底部音符/频谱动效。
  - 已接入自定义 Meting API：`https://api.injahow.cn/meting/?server=netease&type=playlist&id=17929070065`。
  - 可展示歌单列表，点击歌曲切换播放。
  - 上一首/下一首会按歌单切歌。
- 设置面板：
  - 支持显示时间、显示图标、搜索新标签页打开开关。
  - 支持背景、音乐、Meting API、外观参数、导入导出配置。
  - 设置面板和歌单列表滚动条已开始改成暗粉像素风，贴合整体 UI。

## 下一步要修改的内容

- 音乐播放器补充播放进度条：
  - 在右下角小窗口中显示当前播放进度、当前时间、总时长。
  - 支持点击或拖动进度条跳转播放位置。
  - 进度条视觉要贴合暗粉像素风，并和底部频谱/音符动效协调。
- 完善音乐播放器 UI：
  - 检查歌单列表滚动条在不同屏幕高度下是否自然。
  - 优化歌曲标题、作者过长时的截断与排版。
  - 根据播放状态让音符和频谱更明显但不遮挡主体。
- 完善设置滚动区域：
  - 检查设置面板在小屏幕下的最大高度、滚动条位置和按钮可见性。
  - 让文件上传、URL 输入、Meting API 输入和外观滑块在滚动时保持统一间距。
- Git 提交：
  - 当前用户要求对项目进行 git 提交。
  - 提交前需要检查 `git status`，确认只提交本项目相关文件。
  - 建议提交信息：`feat: build pixel new tab page`。
- 视觉细节后续可调：
  - 暗粉蒙尘遮罩的透明度和颜色可以继续按参考图微调。
  - 音乐小窗口可以进一步做成更接近参考图的横向底部播放条模式。
