# Pixel New Tab Project Notes

## 编码与文本安全

- 后续所有 agent 修改文件前必须先阅读并遵循 `ENCODING.md`。
- 本项目文本文件统一使用 UTF-8 without BOM，禁止保存为 ANSI、GBK、GB2312、Big5 或 UTF-16。
- 中文 UI 文案必须保持可读中文；提交或交接前建议扫描常见乱码痕迹。

## 当前系统架构/模块划分

- `index.html`：单页新标签页入口，负责页面结构。包含首屏设置预加载脚本、像素/发光时间、搜索框、快捷图标区、设置抽屉、添加/编辑图标弹窗、小角色、音乐播放器、底部音频动效、动态歌词浮层等主要 DOM。
- `styles.css`：全局视觉系统和响应式样式。包含像素风背景、暗粉蒙尘遮罩、搜索引擎下拉、快捷图标、设置面板、音乐窗口、歌单列表、滚动条、小角色、流星和底部云层、底部音阶、点击蓝色爱心特效、动态歌词等 UI 样式。
- `app.js`：全部交互逻辑。包含时间渲染、搜索引擎选择、搜索跳转、设置持久化、背景上传与 IndexedDB 存储、快捷图标增删改查、右键菜单、小角色随机移动/拖拽、鼠标指针、点击特效、音乐播放、Meting 歌单/歌词拉取与切歌、封面显示、音阶动画、配置导入导出等功能。
- `assets/`：本地静态资源目录。
  - `assets/pet/`：小角色 gif。
  - `assets/cursor/`：自定义鼠标 cursor 文件和转换后的 32px PNG/cur 指针资源。
  - `assets/effects/blue-heart.png`：点击特效使用的透明背景蓝色爱心抠图资源。
- 本地数据存储：
  - `localStorage`：保存搜索引擎、显示开关、快捷图标、外观参数、音乐链接、Meting API 地址等轻量配置。
  - `IndexedDB`：保存用户上传的原图背景，避免大图塞入 `localStorage` 导致失败。

## 已完成的功能清单

- 像素风新标签页基础界面：背景、暗粉蒙尘遮罩、流星/星星动画、像素云层。
- 首屏防闪动：
  - `index.html` 头部内联脚本会在 CSS 加载前读取 `pixelNewTab.appearance` 等设置。
  - 提前写入时间、搜索框、快捷图标的透明度、缩放和位置 CSS 变量。
  - 提前应用时间、快捷图标、底部云朵、底部音阶等显示/隐藏状态，避免刷新时先显示默认位置再瞬移。
- 搜索功能：
  - 支持 Google、Bing、Baidu、DuckDuckGo 切换。
  - 支持设置搜索结果在当前页或新标签页打开。
  - 搜索引擎选择 UI 已改成参考图风格的圆角像素下拉菜单。
- 时间显示：
  - 已从原像素点阵渲染改为真实文本渲染，便于字体和发光效果生效。
  - 当前样式为柔粉发光手写风，带细描边、半透明叠层、雾光 bloom，以及 `Fallen / Petals` 小装饰字。
  - 可在设置里显示/隐藏，并支持调节透明度、大小和位置。
- 快捷图标：
  - 可添加网址快捷方式。
  - 支持名称、网址、图标颜色、文字图标、上传图标、快捷键。
  - 支持右键菜单打开、修改、删除。
  - “显示图标”开关会隐藏/显示整个快捷图标区域。
- 背景自定义：
  - 默认背景为导出配置内置壁纸 `assets/wallpapers/default-config.png`，壁纸选择中仍保留 `suihua.png` 等多张内置图。
  - 移动端默认背景为 `assets/wallpapers/mobile-default.jpg`，小屏首屏默认让时间和搜索框居中显示。
  - 设置面板包含“内置壁纸”网格，可在项目内置的多张壁纸中切换。
  - 支持上传本地图片作为背景。
  - 支持图片 URL。
  - 上传原图保存到 IndexedDB。
  - 支持配置导出/导入迁移背景和设置。
- 小角色：
  - `move.gif` 在主页随机飞行移动。
  - 移动一段时间后随机切换为 `seal.gif` 或 `sigh.gif`。
  - 点击变成 `stare.gif`。
  - 拖拽变成 `excited.gif`。
  - 飞行方向会根据移动方向左右镜像。
- 鼠标指针：
  - 页面内使用自定义普通指针。
  - 拖拽时切换为 Alternate 指针。
  - 已用 32px PNG/cur 降低过大问题。
- 点击特效：
  - 页面点击时生成蓝色爱心和柔光拖尾粒子。
  - 爱心资源来自 `assets/effects/blue-heart.png`，已处理为透明背景，只保留蓝色玻璃爱心本体。
  - 爱心点击后会散开、漂浮并慢慢淡出。
- 音乐播放器：
  - 右下角音乐播放小窗口。
  - 可折叠/展开，折叠后保留音符按钮。
  - 支持直接音频 URL。
  - 支持本地音频文件临时播放。
  - 已接入自定义 Meting API：`https://api.injahow.cn/meting/?server=netease&type=playlist&id=17929070065`。
  - 可展示歌单列表，点击歌曲切换播放。
  - 上一首/下一首会按歌单切歌。
  - 已补充播放进度条、当前时间、总时长，并支持点击或拖动跳转播放位置。
  - 已优化歌单在小屏幕高度下的滚动高度，以及歌曲标题/作者过长时的截断排版。
  - 自动获取并显示歌曲封面。
  - 封面右侧有随播放状态出现的专辑小音阶；未播放时隐藏，避免假律动。
- 歌词与音阶：
  - 支持从 Meting 歌单字段或 `type=lyric` / `type=lrc` 歌词接口获取歌词。
  - 兼容 `lyric`、`lyrics`、`lrc`、`lrcUrl`、`lyricUrl` 等字段；`lrc` 为 URL 时会再次 fetch 解析。
  - 动态歌词按播放时间触发，以粉色发光文字随机大小和随机位置出现，渐入渐出。
  - 暂停时不生成新歌词，切歌或换音源时清空旧歌词。
  - 底部音阶只在播放时显示，并可在设置中单独关闭。
  - “显示底部音阶”开关只控制底部频谱，不会关闭底部漂浮音符。
  - 底部音阶已调整为较克制的优雅风格：细柱、低发光、多层慢波律动。
  - 为降低播放音乐时的 GPU 占用，播放态不再运行逐帧 DOM 频谱和全屏流星；音符与频谱动效保留为 CSS transform/opacity 动画。
- 设置面板：
  - 支持显示时间、显示图标、搜索新标签页打开、显示底部云朵、显示底部音阶开关。
  - 支持背景、音乐、Meting API、外观参数、导入导出配置。
  - 设置面板和歌单列表滚动条已改成暗粉像素风，贴合整体 UI。
  - 已优化小屏幕最大高度、滚动条位置、按钮换行可见性和表单间距。
  - 已移除系统默认白色滑块观感，统一为暗粉像素风 range 样式。
  - 设置面板已改为靠右抽屉展示，方便实时观察主页调整效果。
  - 已添加遮罩强度滑块，0 为无遮罩，100 为当前粉色遮罩强度。

## 当前注意事项

- 当前工作区已有未提交改动。用户要求提交前，需要先检查 `git status`，确认只提交本项目相关文件。
- 建议提交信息仍可使用：`feat: build pixel new tab page`。
- `AGENTS.md` 已重新整理为 UTF-8 中文说明，替代此前乱码内容。

## 后续可调方向

- 音乐小窗口可以进一步做成更接近参考图的横向底部播放条模式。
- 时间字体如果需要完全贴近参考图，需要引入或制作专用字体资源；当前版本主要靠系统字体栈和 CSS 光效模拟。
