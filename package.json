{
  "name": "pixel-new-tab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check:js": "node --check app.js && node --check src/state/settings.js",
    "test:visual": "node scripts/visual-check.mjs"
  },
  "devDependencies": {
    "playwright": "^1.57.0"
  }
}
